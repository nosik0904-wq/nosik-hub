# Voice Guide

Canonical copy lives in `shared/voice.js`.

Do not add new product copy directly in HTML, Hub JS, App JS or DB helpers. Add or adjust voice strings in `shared/voice.js` first, then reference them from the UI.
